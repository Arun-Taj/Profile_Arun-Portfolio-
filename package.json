{
  "name": "portfolio-mern-vite",
  "version": "2.0.0",
  "description": "Full Stack Developer Portfolio — React + Vite + Tailwind CSS + Node.js + MongoDB",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "mongod": "^2.0.0",
    "mongodb": "4.1",
    "react-icons": "^5.6.0"
  }
}
